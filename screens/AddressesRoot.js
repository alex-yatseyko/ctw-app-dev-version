import { createStackNavigator } from '@react-navigation/stack';

const Stack = createStackNavigator();

export const AddressesRoot = () => {
    return(
        <Stack.Navigator>
            <Stack.Screen 
                
            />
        </Stack.Navigator>
    )
}

// - + in Header goes to New Address 
// - Addresses
// - Single Address
// - Edit Address